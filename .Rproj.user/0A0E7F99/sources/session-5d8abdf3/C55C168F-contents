setMethod("add", "matrix",
###########################################

function(input, other, alpha = 1) {
  # dimension check
  if(
    dim(input) != dim(other)
  ){
    stop("Cannot add")
  # operation
  }else{
    return(input + alpha * other)
  }
  }

###########################################
)
