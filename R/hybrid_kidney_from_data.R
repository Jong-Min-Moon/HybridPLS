library(Matrix)
library(MASS)
library(pracma)
library(jpeg)
library(fda)
library(splines2)

################# Read functions for kidney data ###################

hybrid_kidney_from_data <- function(
    argvals_base,
    argvals_post,
    reno_base,
    reno_post,
    scalar_predictors,
    n_basis
){


  return(hybrid_predictor_dataset)
}
